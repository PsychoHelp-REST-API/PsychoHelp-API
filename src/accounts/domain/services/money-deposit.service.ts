import { Account } from "../entities/account.entity";
import { Money } from "../../../common/domain/value-objects/money.value";

export class MoneyDepositService {
  public deposit(toAccount: Account, amount: Money){

  }
}